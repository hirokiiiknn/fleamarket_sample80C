= render partial: "items/header"

- content_for :css do
  = stylesheet_link_tag 'modules/_done'

.user
  .done
    .done__title
      クレジット情報を登録しました
      
= render partial: "items/banner"
= render partial: "items/footer"
