- content_for :css do
  = stylesheet_link_tag 'modules/pay'

= render partial: "items/header"
.done#fullsize
  .done__title
    購入手続が完了しました
  .done__backlink
    = link_to 'トップページに戻る', root_path, class: 'link'
  .done__backlink
    = link_to 'マイページに戻る', user_path(current_user.id), class: 'link'

    -# "残数は
    -# = @item.quantity
    -# 個です。"

= render partial: "items/banner"

= render partial: "items/footer"